# OUTPUT — one hypothesis

A run writes exactly one file, `hypothesis.json`, holding **one hypothesis** —
the one that ranked first. Not a slate, not a list.

Many candidates are enumerated, scored, critiqued and ranked on the way there,
because that is how the winner is known. Only the winner crosses this boundary.
If nothing survives selection the run writes nothing and says so: an empty
answer with a clear next step is a real answer, and it is a better one than the
least bad candidate promoted to look like a finding.

Schema version **2.0**. Adding an optional field is a minor bump;
removing one, or narrowing what it may contain, is a major one.

## What makes the document self-contained

`provenance` travels with the claim because a hypothesis separated from what
produced it cannot be judged. Support 0.5 from a cautious run and support 0.5
from an ambitious one are not the same statement about the world, and the
number alone cannot tell them apart — so the stance, the coverage of the graph
it read, and how many candidates it beat all ship in the same file.

## Reading order for a consumer

1. `schema_version` — know what you are parsing.
2. `provenance.coverage` — how hard the graph was searched. Read this *before*
   the scores. Absence of evidence is not evidence of absence.
3. `hypothesis.verification.verdict` — one of `verified`, `qualified`,
   `unverified`, `rejected`. A verdict of `unverified` with `halted_at` set
   means gates below the halt **did not run**; they are not passes.
4. `hypothesis.issues` — anything at severity `error` means this is not
   presentable as a finding.
5. Only then, the claim itself.

## The rules the document promises

- Every id in `hypothesis.evidence` and in every claim's `cites` appears in the
  input graph. A citation outside what the model was shown is rejected before
  it can reach this file.
- `hypothesis.scores` is a vector, not a ranking. Support and novelty are
  separate axes on purpose: averaging them puts textbook statements first.
- `asks` names the exact request that would move this hypothesis, keyed by id,
  in the graph builder's own request shape. No prose to interpret.

Machine-readable form: [`schemas/hypothesis.schema.json`](../../schemas/hypothesis.schema.json).

<!-- generated by tools/generate_schemas.py — edit the prose above, not the tables -->

## HypothesisDocument

THE OUTPUT. One run over one knowledge graph produces exactly one of these.

Written as ``hypothesis.json``. This is a single hypothesis -- the one that
ranked first -- not a slate. A run still enumerates, scores and ranks many
candidates internally, because that is how it knows which one is best, but
only the winner crosses this boundary.

The document is self-contained on purpose: ``provenance`` says what it was
run on and with what stance, ``hypothesis`` is the claim with its evidence
and its verification, and ``asks`` names the requests that would move it.
Nothing here needs the graph to be re-read to be understood.

Reading order for a consumer: ``schema_version``, then
``provenance.coverage``, then ``hypothesis.verification.verdict``, then the
claim itself. A hypothesis carrying an error-level issue is not a finding.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | `string` | no | Version of the hypothesis.json contract this file was written against. |
| `provenance` | [Provenance](#provenance) | **yes** | What produced this hypothesis. |
| `hypothesis` | [Hypothesis](#hypothesis) | **yes** | The hypothesis itself: one claim, with its evidence. |
| `asks` | list of [Ask](#ask) | no | Requests back to the graph builder that would move this hypothesis. |

## Articulation

What the model is asked to produce from one structural candidate.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `statement` | `string` | **yes** | The hypothesis in one testable sentence. |
| `mechanism` | `string` | **yes** | The proposed causal chain, in graph terms. |
| `claims` | list of [Claim](#claim) | **yes** | The hypothesis decomposed into checkable pieces. |
| `novel_because` | `string` | **yes** | What the graph does NOT already state. |
| `predictions` | list of `string` | no | Observations that should hold if this is true. |
| `falsifier` | `string` | **yes** | The single observation that would kill this. |
| `decisive_experiment` | `string` | **yes** | The cheapest experiment that discriminates. |
| `assumptions` | list of `string` | no | What must be true but is not in the graph. |

## Ask

A request back to the graph builder. This is the loop closing.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `graph_id` | `string` | **yes** |  |
| `ask` | `expand_node` \| `resolve_link` \| `test_gap` \| `new_question` | **yes** |  |
| `target` | `string` | **yes** |  |
| `depth` | `quick` \| `standard` \| `deep` \| `exhaustive` | no |  |
| `reason` | `string` | no |  |
| `for_hypothesis` | `string` or null | no |  |

## Claim

One atomic, separately checkable assertion inside a hypothesis.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `text` | `string` | **yes** | A single assertion, stated so it can be true or false on its own. |
| `cites` | list of `string` | no | Ids from the evidence pack only (link, finding, paper, thing, or gap ids). |
| `inferred` | `boolean` | no | True when the graph does not state this and it is a step of reasoning. |

## Critique

The adversarial pass. Its job is to break the hypothesis, not polish it.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `verdict` | `supported` \| `partly_supported` \| `unsupported` \| `contradicted` | **yes** |  |
| `strongest_objection` | `string` | **yes** |  |
| `unsupported_leaps` | list of `string` | no |  |
| `per_claim` | list of [CritiqueFinding](#critiquefinding) | no |  |
| `alternative_explanation` | `string` | no | A duller reading of the same evidence, if one exists. |
| `lens` | `string` | no | Which angle this critic was told to attack from. Set by the harness, not the model. |

## CritiqueFinding

| Field | Type | Required | Meaning |
|---|---|---|---|
| `claim_index` | `integer` | **yes** |  |
| `verdict` | `supported` \| `partly_supported` \| `unsupported` \| `contradicted` | **yes** |  |
| `reason` | `string` | **yes** |  |
| `cites` | list of `string` | no |  |

## GateResult

The outcome of one verification gate.

``summary`` is the one line that appears in the gate table, so it must say
what happened rather than what was checked: "L4,L2 share first author — 1
group" is a result, "checked independence" is not.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `name` | `string` | **yes** |  |
| `status` | `pass` \| `warn` \| `fail` \| `skip` | **yes** |  |
| `summary` | `string` | no |  |
| `issues` | list of [ValidationIssue](#validationissue) | no |  |
| `halting` | `boolean` | no | Whether a failure here stops the process. Set from params, not by the gate. |

## Hypothesis

One fully assembled, inspectable hypothesis.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `id` | `string` | **yes** |  |
| `motif` | `string` | **yes** |  |
| `subject` | `string` | **yes** |  |
| `object` | `string` | **yes** |  |
| `subject_name` | `string` | **yes** |  |
| `object_name` | `string` | **yes** |  |
| `hops` | `integer` | **yes** |  |
| `tags` | list of `string` | no |  |
| `path` | list of object | no |  |
| `scores` | object of `number` | no |  |
| `rank_score` | `number` | no |  |
| `evidence` | object | no |  |
| `caveats` | list of `string` | no |  |
| `articulation` | [Articulation](#articulation) or null | no |  |
| `critiques` | list of [Critique](#critique) | no |  |
| `verdict` | `supported` \| `partly_supported` \| `unsupported` \| `contradicted` or null | no | Consensus across critics, per refute_threshold. |
| `verification` | [Verification](#verification) or null | no | The staged gate process. None means it never ran. |
| `elo` | `number` or null | no | Set only when the tournament ran. |
| `evolved_from` | `string` or null | no |  |
| `evolution_operator` | `string` or null | no |  |
| `issues` | list of [ValidationIssue](#validationissue) | no |  |
| `asks` | list of [Ask](#ask) | no |  |
| `provenance` | `string` | no |  |

## Provenance

What the run was, so the hypothesis can be checked without the run.

A hypothesis separated from what produced it is unfalsifiable in practice:
support 0.5 from a cautious run and support 0.5 from an ambitious one are
not the same claim about the world, and the score alone cannot tell them
apart. So every document carries its own.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `graph_id` | `string` | **yes** | Id of the input knowledge graph this run read. |
| `round` | `integer` | **yes** | The graph's round number, copied from the input. |
| `question` | `string` | **yes** | The question the input graph was built to answer. |
| `generated_at` | `string` or null | no | ISO 8601 timestamp of the run, if recorded. |
| `params` | object | no | The full resolved parameters. With the graph, these determine the output. |
| `coverage` | object | no | What the input graph says about how thoroughly it searched. Read this before the scores: absence of evidence is not evidence of absence. |
| `counts` | object of `integer` | no | Run tallies: graph size, how many candidates were considered, model calls. |
| `considered` | `integer` | no | How many hypotheses this run assembled and ranked before choosing the one in this document. Selection is real work; a reader should know it happened. |

## ValidationIssue

| Field | Type | Required | Meaning |
|---|---|---|---|
| `code` | `string` | **yes** |  |
| `detail` | `string` | **yes** |  |
| `severity` | `error` \| `warning` | no |  |

## Verification

The full staged verification of one hypothesis.

Distinct from ``Hypothesis.verdict``, which is only the adversarial
critics' consensus. This is the whole process: what ran, what it found, and
where it stopped.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `verdict` | `verified` \| `qualified` \| `unverified` \| `rejected` | **yes** |  |
| `gates` | list of [GateResult](#gateresult) | no |  |
| `halted_at` | `string` or null | no | Name of the gate that stopped the process, if one did. |
